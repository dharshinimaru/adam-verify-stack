export default function Disclaimer() {
  return (
    <p className="text-xs text-center mt-4 py-2 px-3 rounded-lg border" style={{ color: "var(--text-muted)", borderColor: "var(--border-subtle)", background: "var(--bg-surface)" }}>
      Decision support only — final release decisions require human QA review.
    </p>
  );
}
